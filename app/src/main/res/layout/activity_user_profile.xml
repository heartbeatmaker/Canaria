<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".UserProfileActivity">

    <ImageView
        android:id="@+id/userProfile_friendImage_imageView"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:background="@drawable/ic_person_black_24dp"
        android:layout_marginTop="300dp"
        android:layout_centerHorizontal="true"/>

    <TextView
        android:id="@+id/userProfile_friend_username"
        android:layout_width="wrap_content"
        android:layout_height="22dp"
        android:text="Name"
        android:textColor="@color/black"
        android:textSize="18sp"
        android:layout_centerHorizontal="true"
        android:layout_below="@id/userProfile_friendImage_imageView"
        android:layout_above="@+id/userProfile_linearLayout"/>

    <LinearLayout
        android:layout_alignParentBottom="true"
        android:id="@+id/userProfile_linearLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <Button
            android:id="@+id/userProfile_startChat_btn"
            android:layout_width="match_parent"
            android:layout_height="100dp"
            android:text="Chat" />

    </LinearLayout>
</RelativeLayout>